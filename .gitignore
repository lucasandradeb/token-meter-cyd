# PlatformIO
.pio/
firmware/.pio/
bringup/.pio/

# Python daemon
venv/
.venv/
__pycache__/
*.pyc

# Secrets / credenciais (nunca commitar token OAuth)
*.env
.env
secrets.json
.device_credentials.json
daemon/.device_credentials.json
.device_login_state.json
daemon/.device_login_state.json

# Imagem do topo gerada localmente (pode conter asset de terceiros; nao commitar)
firmware/src/app/party_img_user.c
firmware/src/app/party_img_user.h

# macOS
.DS_Store

# VSCode (manter .vscode/extensions.json se quiser recomendar PlatformIO)
.vscode/*
!.vscode/extensions.json
